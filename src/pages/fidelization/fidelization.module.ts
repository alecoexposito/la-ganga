import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FidelizationPage } from './fidelization';

@NgModule({
  declarations: [
  ],
  imports: [
    IonicPageModule.forChild(FidelizationPage),
  ],
})
export class FidelizationPageModule {}
